import { FilterActionTakenPipe } from './filter-action-taken.pipe';

describe('FilterActionTakenPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActionTakenPipe();
    expect(pipe).toBeTruthy();
  });
});
