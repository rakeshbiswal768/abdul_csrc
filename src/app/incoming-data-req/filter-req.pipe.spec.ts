import { FilterReqPipe } from './filter-req.pipe';

describe('FilterReqPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReqPipe();
    expect(pipe).toBeTruthy();
  });
});
